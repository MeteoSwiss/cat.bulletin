
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$high_prec_count_hr == 1, "station", "stations")
ordnum_rank <- ifelse(regdiff$high_prec_highest_rank == 1, "er", "e")
month_with_art <- add_article(month_str(bulletin$month), lang="fr", to_lower=TRUE)
num_word <- num_to_word(regdiff$high_prec_count_hr, "fr")
record_list <- translate_record_text(regdiff$high_prec_station_record_info, "fr")
```

`r ifelse(regdiff$high_prec_count_hr <= 5, glue::glue("Le cumul mensuel des précipitations en {tolower(month_str(bulletin$month))} {bulletin$year} a atteint dans {num_word} {station_word} disposant de données depuis plus de {regdiff$high_prec_shortest_period} années le {regdiff$high_prec_highest_rank}{ordnum_rank} rang du mois {month_with_art} le plus arrosé : {record_list}."), glue::glue("Le cumul mensuel des précipitations en {tolower(month_str(bulletin$month)} {bulletin$year} a atteint dans {num_word} {station_word} disposant de données depuis plus de {regdiff$high_prec_shortest_period} années le {regdiff$high_prec_highest_rank}{ordnum_rank} rang du mois {month_with_art} le plus arrosé."))``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr==1, " Il s'agit d'une valeur record provisoire. La valeur mesurée, le rang et le nombre de stations ayant établi des records peuvent encore changer d'ici la fin du mois.", "")``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr>1, " Il s'agit de valeurs record provisoires. Les valeurs mesurées, les rangs et le nombre de stations ayant établi des records peuvent encore changer d'ici la fin du mois.", "")`

