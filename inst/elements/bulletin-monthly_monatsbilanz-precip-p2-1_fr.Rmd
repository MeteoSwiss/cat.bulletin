
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$high_prec_count_hr == 1, "Messstation", "Messstationen")
```

`r ifelse(regdiff$high_prec_count_hr <= 5, glue::glue("Die Monatssumme des Niederschlags im {bulletin$month_str} {bulletin$year} erreichte an {regdiff$high_prec_num_word} {station_word} mit Daten seit über {regdiff$high_prec_shortest_period} Jahren den {regdiff$high_prec_highest_rank}. Rang der nassesten {bulletin$month_str}monate: {regdiff$high_prec_station_record_info}."), glue::glue("Die Monatssumme des Niederschlags im {bulletin$month_str} {bulletin$year} erreichte an {regdiff$high_prec_num_word} {station_word} mit Daten seit über {regdiff$high_prec_shortest_period} Jahren den {regdiff$high_prec_highest_rank}. Rang der nassesten {bulletin$month_str}monate."))``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr==1, " Das ist ein vorläufiger Rekordwert. Der Messwert, der Rang und die Anzahl der Stationen mit Rekorden kann bis zum Monatsende noch ändern.", "")``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr>1, " Das sind vorläufige Rekordwerte. Die Messwerte, die Ränge und die Anzahl der Stationen mit Rekorden können bis zum Monatsende noch ändern.", "")`

