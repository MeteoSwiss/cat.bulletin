

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$high_temp_count_hr == 1, "Messstation", "Messstationen")
sw <- ifelse(regdiff$high_temp_highest_rank == 1, "waren", "sind")
```

`r ifelse(regdiff$high_temp_count_hr <= 5, glue::glue("Die Monatsmitteltemperatur im August 2024 erreicht an {regdiff$high_temp_count_hr} {station_word} mit Messreihen von über {regdiff$high_temp_shortest_period} Jahren den {regdiff$high_temp_highest_rank}. Rang: {regdiff$high_temp_stations_with_new_temp_recs}. Die bisherigen Monatsrekorde an diesen Messstationen {sw}:  {regdiff$high_temp_old_station_records}."), glue::glue("Die Monatsmitteltemperatur im August 2024 erreicht an {regdiff$high_temp_count_hr} {station_word} mit Messreihen von über {regdiff$high_temp_shortest_period} Jahren den {regdiff$high_temp_highest_rank}. Rang."))`  
