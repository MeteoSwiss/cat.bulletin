
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$low_prec_count_hr == 1, "stazione", "stazioni")
rank_it <- ifelse(regdiff$low_prec_highest_rank == 1, "", paste0(regdiff$low_prec_highest_rank,"° "))
num_word <- num_to_word(regdiff$low_prec_count_hr, "it")
```


`r ifelse(regdiff$low_prec_count_hr <= 5, glue::glue("Il totale mensile delle precipitazioni in {tolower(month_str(bulletin$month))} {bulletin$year} è risultato in {num_word} {station_word} di rilevamento con dati da oltredi rilevamento con dati da oltre {regdiff$low_prec_shortest_period} anni il {rank_it}più basso per {tolower(month_str(bulletin$month))}: {regdiff$low_prec_station_record_info}."), glue::glue("Il totale mensile delle precipitazioni in {tolower(month_str(bulletin$month))} {bulletin$year} è risultato in {num_word} {station_word} di rilevamento con dati da oltredi rilevamento con dati da oltre {regdiff$low_prec_shortest_period} anni il {rank_it}più basso per {tolower(month_str(bulletin$month))}."))``r ifelse(bulletin$provisional & regdiff$low_prec_count_hr==1, " Si tratta di un record provvisorio. Il valore misurato, la posizione in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")``r ifelse(bulletin$provisional & regdiff$low_prec_count_hr>1, " Questi sono valori record provvisori. I valori misurati, le posizioni in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")`

