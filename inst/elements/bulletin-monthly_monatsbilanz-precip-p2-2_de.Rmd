
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$low_prec_count_hr == 1, "Messstation", "Messstationen")
num_word <- num_to_word(regdiff$low_prec_count_hr, "de")
```


`r ifelse(regdiff$low_prec_count_hr <= 5, glue::glue("Die Monatssumme des Niederschlags im {month_str(bulletin$month)} {bulletin$year} erreichte an {num_word} {station_word} mit Daten seit über {regdiff$low_prec_shortest_period} Jahren den {regdiff$low_prec_highest_rank}. Rang der niederschlagsärmsten {month_str(bulletin$month)}monate: {regdiff$low_prec_station_record_info}."), glue::glue("Die Monatssumme des Niederschlags im {month_str(bulletin$month)} {bulletin$year} erreichte an {num_word} {station_word} mit Daten seit über {regdiff$low_prec_shortest_period} Jahren den {regdiff$low_prec_highest_rank}. Rang der niederschlagsärmsten {month_str(bulletin$month)}monate."))``r ifelse(bulletin$provisional & regdiff$low_prec_count_hr==1, " Das ist ein vorläufiger Rekordwert. Der Messwert, der Rang und die Anzahl der Stationen mit Rekorden kann bis zum Monatsende noch ändern.", "")``r ifelse(bulletin$provisional & regdiff$low_prec_count_hr>1, " Das sind vorläufige Rekordwerte. Die Messwerte, die Ränge und die Anzahl der Stationen mit Rekorden können bis zum Monatsende noch ändern.", "")`

