

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$high_temp_count_hr == 1, "stazione", "stazioni")
rank_it <- ifelse(regdiff$high_temp_highest_rank == 1, "", paste0(regdiff$high_temp_highest_rank,"&ordf; "))
```

`r ifelse(regdiff$high_temp_count_hr <= 5, glue::glue("La temperatura media mensile di {month_str(bulletin$month)} {bulletin$year} è risultata in {regdiff$high_temp_num_word} {station_word} con dati da più di {regdiff$high_temp_shortest_period} anni la {rank_it}più elevata per {month_str(bulletin$month)}: {regdiff$high_temp_station_record_info}."), glue::glue("La temperatura media mensile di {month_str(bulletin$month)} {bulletin$year} è risultata in {regdiff$high_temp_num_word} {station_word} con dati da più di {regdiff$high_temp_shortest_period} anni la {rank_it}più elevata per {month_str(bulletin$month)}."))``r ifelse(bulletin$provisional & regdiff$high_temp_count_hr==1, " Si tratta di un record provvisorio. Il valore misurato, la posizione in classifica e il numero di stazioni con record possono ancora cambiare entro la fine del mese.", "")``r ifelse(bulletin$provisional & regdiff$high_temp_count_hr>1, " Questi sono valori record provvisori. I valori misurati, le posizioni in classifica e il numero di stazioni con record possono ancora cambiare entro la fine del mese.", "")`
