
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$low_sun_count_hr == 1, "stazione", "stazioni")
rank_it <- ifelse(regdiff$low_sun_highest_rank == 1, "", paste0(regdiff$low_sun_highest_rank,"° "))
```

`r ifelse(regdiff$low_sun_count_hr <= 5, glue::glue("Il numero totale di ore di sole di {tolower(month_str(bulletin$month))} {bulletin$year} è risultato in {regdiff$low_sun_num_word} {station_word} di rilevamento attive da oltre {regdiff$low_sun_shortest_period} anni il {rank_it} più piccolo per {tolower(month_str(bulletin$month))}: {regdiff$low_sun_station_record_info}."), glue::glue("Il numero totale di ore di sole di {tolower(month_str(bulletin$month))} {bulletin$year} è risultato in {regdiff$low_sun_num_word} {station_word} di rilevamento attive da oltre {regdiff$low_sun_shortest_period} anni il {rank_it} più piccolo per {tolower(month_str(bulletin$month))}."))``r ifelse(bulletin$provisional & regdiff$low_sun_count_hr==1, " Si tratta di un record provvisorio. Il valore misurato, la graduatoria e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")``r ifelse(bulletin$provisional & regdiff$low_sun_count_hr>1, " Questi sono valori record provvisori. I valori misurati, le posizioni in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")`
