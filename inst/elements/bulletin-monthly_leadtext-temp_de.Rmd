``` {r echo=FALSE, results = "hide"}
moment01 <- ifelse(bulletin$provisional, "kurz vor Monatsende voraussichtlich ", "")
abw_lwt <- format(abs(as.numeric(swissmean$curr_temp_dev)),nsmall=1)

sm_gt0 <- as.numeric(swissmean$curr_temp_dev) > 0
sm_eq0 <- as.numeric(swissmean$curr_temp_dev) == 0
sm_lt0 <- as.numeric(swissmean$curr_temp_dev) < 0
```

`r ifelse(sm_gt0, glue::glue("Die Schweizer Mitteltemperatur im {bulletin$month_str} {bulletin$year} liegt {abw_lwt}&nbsp;°C über der Referenzperiode 1991-2020 und belegt damit {moment01}den {swissmean$curr_rank}. Rang seit Messbeginn 1864."), "")``r ifelse(sm_lt0, glue::glue("Die Schweizer Mitteltemperatur im {bulletin$month_str} {bulletin$year} liegt {abw_lwt}&nbsp;°C unter der Referenzperiode 1991-2020 und belegt damit {moment01}den {swissmean$curr_rank}. Rang seit Messbeginn 1864."), "")``r ifelse(sm_eq0, glue::glue("Die Schweizer Mitteltemperatur im {bulletin$month_str} {bulletin$year} beträgt {swissmean$curr_temp}&nbsp;°C, was genau dem Referenzwert der Periode 1991-2020 und dem {swissmean$curr_rank}. Rang entspricht."), "")`