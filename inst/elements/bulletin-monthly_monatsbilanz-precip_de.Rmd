

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

### Niederschlag

``` {r echo=FALSE, results = "hide"}
minval <- min(regdiff$allvalues_R)
maxval <- max(regdiff$allvalues_R)
```

`r if (all(regdiff$allvalues_R > 105)) {paste0("Die monatliche Niederschlagssumme im ",bulletin$month_str," ",bulletin$year," war schweizweit über der Norm 1991-2020.")}``r if (all(regdiff$allvalues_R < 95)) {paste0("Die Niederschlagsmengen im ",bulletin$month_str," ",bulletin$year," lagen schweizweit unter der Norm 1991-2020.")}``r if (all(regdiff$allvalues_R <= 105) & all(regdiff$allvalues_R >= 95)) {paste0("Die Monatsniederschläge im ",bulletin$month_str," ",bulletin$year," waren im ganzen Land im Bereich der Norm 1991-2020.")}``r if (regdiff$anteil_ueber_R > 0.7 & regdiff$anteil_ueber_R < 1) {paste0("Die monatliche Niederschlagssumme im ",bulletin$month_str," ",bulletin$year," war verbreitet über der Norm 1991-2020.")}``r if (regdiff$anteil_unter_R > 0.7 & regdiff$anteil_unter_R < 1) {paste0("Die Niederschlagssumme im ",bulletin$month_str," ",bulletin$year," lag verbreitet unter der Norm 1991-2020.")}``r if (regdiff$anteil_bereich_R > 0.7 & regdiff$anteil_bereich_R < 1) {paste0("Die Monatsniederschläge im ",bulletin$month_str," ",bulletin$year," waren verbreitet im Bereich der Norm 1991-2020.")}` An Messstandorten von MeteoSchweiz wurden Niederschlagsmengen zwischen `r minval` und `r maxval`&nbsp;% der Norm registriert, wobei ein Grossteil der Stationen im Bereich von `r regdiff$quantiles_R[1]` bis `r regdiff$quantiles_R[2]`&nbsp;% des Normniederschlags lag.

<!-- Hier noch Infos zu absoluten Niederschlagsmengen und deren Verteilung im Raum -->

Die grössten Niederschlagsmengen im Vergleich zur Norm 1991-2020 `r if (length(regdiff$numb_stats_high_R)==1) {"verzeichnet die Region"} else {"verzeichnen die Regionen"}` `r regdiff$regshigh_R`. Beispiele für Messstandorte mit vergleichweise grossen Niederschlagssummen sind `r regdiff$selhigh_stats_R` (siehe Tabelle 2). Die geringsten Niederschlagsmengen im Vergleich zur Norm `r if (length(regdiff$numb_stats_low_R)==1) {"stammen aus der Region"} else {"stammen aus den Regionen"}` `r regdiff$regslow_R` (z.B. die Stationen `r regdiff$sellow_stats_R`).

`r if(any(regdiff$ranky100_R_wet==1,na.rm=TRUE)) {paste0("Der ",bulletin$month_str," war lokal der nasseste seit Messbeginn vor über 100 Jahren  (am Messstandort ",collapse_sentence(mchdwh::station_info(nat_abbr=regdiff$climtab_vals$Station[regdiff$rank1_longseries_R_wet])$station_name),").")}``r if (any(regdiff$ranky100_R_wet %in% 2:3,na.rm=TRUE) & !any(regdiff$ranky100_R_wet == 1,na.rm=TRUE)) {if (length(which(regdiff$ranky100_R_wet %in% 2:3))>5) {paste0("An insgesamt ",length(which(regdiff$ranky100_R_wet %in% 2:3))," Messstandorten mit über 100-jährigen Messreihen gehörte der ",bulletin$month_str," ",bulletin$year," zu den drei nassesten seit Messbeginn.")} else {paste0("Lokal wurde einer der drei nassesten ",bulletin$month_str,"monate verzeichnet (",collapse_sentence(mchdwh::station_info(nat_abbr=regdiff$climtab_vals$Station[which((regdiff$climtab_vals$Rank_R_wet %in% 2:3) & (regdiff$climtab_vals$firstmeas_R < (bulletin$year-100)))])$station_name),").")} }``r if (any(regdiff$ranky100_R_wet %in% 4:5,na.rm=TRUE) & !any(regdiff$ranky100_R_wet %in% 1:3)) {if (length(which(regdiff$ranky100_R_wet %in% 4:5))>5) {paste0("An ",length(which(regdiff$ranky100_R_wet %in% 4:5))," Messstationen mit über 100-jährigen Messreihen gehörte der ",bulletin$month_str," ",bulletin$year," zu den fünf nassesten seit Messbeginn.")} else {paste0("Lokal wurde einer der fünf nassesten ",bulletin$month_str,"monate verzeichnet (",collapse_sentence(mchdwh::station_info(nat_abbr=regdiff$climtab_vals$Station[which((regdiff$climtab_vals$Rank_R_wet %in% 4:5) & (regdiff$climtab_vals$firstmeas_R < (bulletin$year-100)))])$station_name),").")} }`

`r if(any(regdiff$ranky100_R_dry==1,na.rm=TRUE)) {paste0("Der ",bulletin$month_str," war lokal der niederschlagsärmste seit Messbeginn vor über 100 Jahren  (am Messstandort ",collapse_sentence(mchdwh::station_info(nat_abbr=regdiff$climtab_vals$Station[regdiff$rank1_longseries_R_dry])$station_name),").")}``r if (any(regdiff$ranky100_R_dry %in% 2:3,na.rm=TRUE) & !any(regdiff$ranky100_R_dry == 1,na.rm=TRUE)) {if (length(which(regdiff$ranky100_R_dry %in% 2:3))>5) {paste0("An insgesamt ",length(which(regdiff$ranky100_R_dry %in% 2:3))," Messstandorten mit über 100-jährigen Messreihen gehörte der ",bulletin$month_str," ",bulletin$year," zu den drei trockensten seit Messbeginn.")} else {paste0("Lokal wurde einer der drei trockensten ",bulletin$month_str,"monate verzeichnet (z.B. am Messstandort ",collapse_sentence(mchdwh::station_info(nat_abbr=regdiff$climtab_vals$Station[which((regdiff$climtab_vals$Rank_R_dry %in% 2:3) & (regdiff$climtab_vals$firstmeas_R < (bulletin$year-100)))])$station_name)," mit Messungen seit über 100 Jahren).")} }``r if (any(regdiff$ranky100_R_dry %in% 4:5,na.rm=TRUE) & !any(regdiff$ranky100_R_dry %in% 1:3)) {if (length(which(regdiff$ranky100_R_dry %in% 4:5))>5) {paste0("An ",length(which(regdiff$ranky100_R_dry %in% 4:5))," Messstationen mit über 100-jährigen Messreihen gehörte der ",bulletin$month_str," ",bulletin$year," zu den fünf niederschlagsärmsten seit Messbeginn.")} else {paste0("Lokal wurde einer der fünf niederschlagsärmsten ",bulletin$month_str,"monate verzeichnet (z.B. am Messstandort ",collapse_sentence(mchdwh::station_info(nat_abbr=regdiff$climtab_vals$Station[which((regdiff$climtab_vals$Rank_R_dry %in% 4:5) & (regdiff$climtab_vals$firstmeas_R < (bulletin$year-100)))])$station_name)," mit Messungen seit über 100 Jahren).")} }`

Abbildung 2 zeigt die monatliche Niederschlagssumme in mm (links) und die Abweichung zum Mittel über die Normperiode 1991-2020 in Prozent der Norm (rechts) für den `r bulletin$month_str` `r bulletin$year`. Die Werte wurden über den Zeitraum vom 1. bis `r get_final_date(bulletin$year,bulletin$month)` berechnet. Lagen die Niederschlagssummen über der Norm, sind die entsprechenden Bereiche in Abbildung 2 (rechts) grün bis blau eingefärbt, bräunliche Gebiete weisen Monatsniederschläge unter dem langjährigen Mittel auf.

```{r echo=FALSE, results='asis'}
caption <- paste0("Monatsniederschläge für den ",bulletin$month_str," an ausgewählten Stationen im Messnetz von MeteoSchweiz. Es ist die aktuelle Monatssumme, der Normwert (1991-2020), die Abweichung in Prozent der Norm, der Rang und das Jahr des Messbeginns angegeben. Der Pfeil beim Rang zeigt die Richtung der Sortierung an. Ein Pfeil von oben herab (\u2193) deutet auf einen nasseren Monat hin, ein Pfeil von unten nach oben (\u2191) auf einen trockeneren Monat. 1\u2193 wäre der nasseste, 1\u2191 der trockenste Monat an diesem Standort.")
knitr::kable(regdiff$subset_climtab_R,caption=caption, align = c("l","c","c","c","c","c"))
```
