
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$high_sun_count_hr == 1, "stazione", "stazioni")
rank_it <- ifelse(regdiff$high_sun_highest_rank == 1, "", paste0(regdiff$high_sun_highest_rank,"° "))
num_word <- num_to_word(regdiff$high_sun_count_hr, "it")
record_list <- translate_record_text(regdiff$high_sun_station_record_info, "it")
```

`r ifelse(regdiff$high_sun_count_hr <= 5, glue::glue("In {num_word} {station_word} di misura con dati da più di {regdiff$high_sun_shortest_period} anni il numero totale di ore di sole di {tolower(month_str(bulletin$month))} {bulletin$year} è risultato il {rank_it}più elevato per {tolower(month_str(bulletin$month))}: {record_list}."), glue::glue("In {num_word} {station_word} di misura con dati da più di {regdiff$high_sun_shortest_period} anni il numero totale di ore di sole di {tolower(month_str(bulletin$month))} {bulletin$year} è risultato il {rank_it}più elevato per {tolower(month_str(bulletin$month))}."))``r ifelse(bulletin$provisional & regdiff$high_sun_count_hr==1, " Si tratta di un record provvisorio. Il valore misurato, la posizione in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")``r ifelse(bulletin$provisional & regdiff$high_sun_count_hr>1, " Questi sono valori record provvisori. I valori misurati, le posizioni in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")`
