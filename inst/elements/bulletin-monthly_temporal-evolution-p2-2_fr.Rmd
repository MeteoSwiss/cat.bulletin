

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$low_temp_count_hr == 1, "station", "stations")
ordnum_rank <- ifelse(regdiff$low_temp_highest_rank == 1, "er", "e")
month_with_art <- add_article(month_str(bulletin$month), lang="fr", to_lower=TRUE)
num_word <- num_to_word(regdiff$low_temp_count_hr, "fr")
```

`r ifelse(regdiff$low_temp_count_hr <= 5, glue::glue("La température moyenne en {tolower(month_str(bulletin$month))} {bulletin$year} a atteint dans {num_word} {station_word} disposant de données depuis plus de {regdiff$low_temp_shortest_period} années le {regdiff$low_temp_highest_rank}{ordnum_rank} rang du mois {month_with_art} le plus froid : {regdiff$low_temp_station_record_info}."), glue::glue("La température moyenne en {tolower(month_str(bulletin$month))} {bulletin$year} a atteint dans {num_word} {station_word} disposant de données depuis plus de {regdiff$low_temp_shortest_period} années le {regdiff$low_temp_highest_rank}{ordnum_rank} rang du mois {month_with_art} le plus froid."))``r ifelse(bulletin$provisional & regdiff$low_temp_count_hr==1, " Il s'agit d'une valeur record provisoire. Il est possible que la valeur mesurée, le rang et le nombre de stations avec des records changent d'ici la fin du mois.", "")``r ifelse(bulletin$provisional & regdiff$low_temp_count_hr>1, " Il s'agit de valeurs record provisoires. Il est possible que les valeurs mesurées, les rangs et le nombre de stations ayant atteint des records changent d'ici la fin du mois.", "")`
