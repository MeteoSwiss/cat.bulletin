
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$high_prec_count_hr == 1, "stazione", "stazioni")
rank_it <- ifelse(regdiff$high_prec_highest_rank == 1, "", paste0(regdiff$high_prec_highest_rank,"° "))
```

`r ifelse(regdiff$high_prec_count_hr <= 5, glue::glue("Il totale mensile delle precipitazioni in {month_str(bulletin$month)} {bulletin$year} è risultato in {regdiff$high_prec_num_word} {station_word} di rilevamento con dati da oltre {regdiff$high_prec_shortest_period} anni il {rank_it}più elevato per {month_str(bulletin$month)}: {regdiff$high_prec_station_record_info}."), glue::glue("Il totale mensile delle precipitazioni in {month_str(bulletin$month)} {bulletin$year} è risultato in {regdiff$high_prec_num_word} {station_word} di rilevamento con dati da oltre {regdiff$high_prec_shortest_period} anni il {rank_it}più elevato per {month_str(bulletin$month)}."))``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr==1, " Si tratta di un record provvisorio. Il valore misurato, la posizione in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr>1, " Questi sono valori record provvisori. I valori misurati, la posizione in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")`

