
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$high_prec_count_hr == 1, "Messstation", "Messstationen")
```

`r ifelse(regdiff$high_prec_count_hr <= 5, glue::glue("Die Monatssumme des Niederschlags im {month_str(bulletin$month)} {bulletin$year} erreichte an {regdiff$high_prec_num_word} {station_word} mit Daten seit über {regdiff$high_prec_shortest_period} Jahren den {regdiff$high_prec_highest_rank}. Rang der nassesten {month_str(bulletin$month)}monate: {regdiff$high_prec_station_record_info}."), glue::glue("Die Monatssumme des Niederschlags im {month_str(bulletin$month)} {bulletin$year} erreichte an {regdiff$high_prec_num_word} {station_word} mit Daten seit über {regdiff$high_prec_shortest_period} Jahren den {regdiff$high_prec_highest_rank}. Rang der nassesten {month_str(bulletin$month)}monate."))``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr==1, " Si tratta di un record provvisorio. Il valore misurato, la posizione in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")``r ifelse(bulletin$provisional & regdiff$high_prec_count_hr>1, " Questi sono valori record provvisori. I valori misurati, la posizione in classifica e il numero di stazioni con record possono ancora cambiare fino alla fine del mese.", "")`

