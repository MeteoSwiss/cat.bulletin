

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r echo=FALSE, results="hide"}
# Determine singular or plural form
station_word <- ifelse(regdiff$low_temp_count_hr == 1, "Messstation", "Messstationen")
rank_it <- ifelse(regdiff$low_temp_highest_rank == 1, "", paste0(regdiff$low_temp_highest_rank,"&ordf; "))
```

`r ifelse(regdiff$low_temp_count_hr <= 5, glue::glue("In {regdiff$low_temp_num_word} {station_word} con dati da più di {regdiff$low_temp_shortest_period} anni la temperatura media mensile di {tolower(month_str(bulletin$month))} {bulletin$year} è risultata la {rank_it}più bassa per {tolower(month_str(bulletin$month))}:  {regdiff$low_temp_station_record_info}."), glue::glue("In {regdiff$low_temp_num_word} {station_word} con dati da più di {regdiff$low_temp_shortest_period} anni la temperatura media mensile di {tolower(month_str(bulletin$month))} {bulletin$year} è risultata la {rank_it}più bassa per {tolower(month_str(bulletin$month))}."))``r ifelse(bulletin$provisional & regdiff$low_temp_count_hr==1, " Questo è un record provvisorio. È possibile che il valore misurato, la posizione in classifica e il numero di stazioni con record cambino ancora dopo la fine del mese.", "")``r ifelse(bulletin$provisional & regdiff$low_temp_count_hr>1, " Si tratta di valori record provvisori. È possibile che i valori misurati, le posizioni in classifica e il numero di stazioni con record cambino ancora dopo la fine del mese.", "")`
